// config for your database, update with your own setup as needed
const config = {
    user: 'postgres',
    password: '',
    server: 'localhost',
    port: 5432,
    database: 'PetsDatabase' 
};

module.exports = config;